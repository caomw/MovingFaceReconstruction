function [S] = LightAndShapeEstimation(M,L,S)

%% Function to estimate surface normals at each pixel in the 3D model


for i = 1:size(M,2)
    
    for j = 1:size(M,1)
        M_j = M(:,i);
        S_j = S(:,i);
   
    dist = (M_j - L*S_j)^2;
    
end

end